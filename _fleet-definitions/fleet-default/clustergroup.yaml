apiVersion: fleet.cattle.io/v1alpha1
kind: ClusterGroup
metadata:
  annotations:
    field.cattle.io/description: 'Install the hello rancher demo'
  name: deploy-hello-rancher
  namespace: fleet-default
spec:
  selector:
    matchLabels:
      deploy-hello-rancher: 'true'
